# Cisco-NAT-PAT-Configuration
Cisco NAT &amp; PAT Configuration project in Cisco Packet Tracer. Demonstrates Network Address Translation (NAT) and Port Address Translation (PAT) for enabling private networks to access external networks using public IP addresses while conserving address space and maintaining connectivity.
